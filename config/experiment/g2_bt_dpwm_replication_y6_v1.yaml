version: g2_bt_dpwm_replication_y6_v1
status: frozen_2026-08-21
v0_config: config/experiment/g2_dual_expert_fair_gate_v0_v1.yaml
seeds: [17, 27]
hidden_dim: 96
contact_conditioned_robot: true
independent_object_encoder: true
block_coordinate_training: true
train_baseline: true
baseline_epochs: 120
baseline_rollout_training_horizon: 5
epochs: 120
robot_epochs: 120
object_epochs: 120
learning_rate: 0.001
joint_refinement_epochs: 0
robot_rollout_training_horizon: 10
object_rollout_training_horizon: 5
primary_domain: D3__mixed_composition
gate:
  minimum_object_improvement_pct: 2.0
  maximum_free_arm_regression_pct: 2.0
  minimum_overall_improvement_pct: 1.0
  maximum_constraint_violation_rms: 1.0e-7
