# Gate A: does a hard joint-lock operator generalize from D2 to held-out D3?
version: g2_topology_surgery_gate_a_v1
status: frozen_2026-08-20
task: push
protocol: config/splits/g2_push_heldout_topology_v1.yaml
targets: config/splits/push_targets_5dof_v1.yaml
methods: [ordinary, soft_topology, topology_surgery]
seeds: [7, 17, 27, 37, 47]
epochs: 20
steps: 150
trajectories_per_train_domain: 2
trajectories_per_test_domain: 3
rollout_horizon: 10
block_initial_xy: [0.24, 0.10]
primary_domain: D3__mixed_composition
primary_metric: object_rmse

# The hard operator trivially fixes locked q/qvel. Passing Gate A additionally
# requires improvements outside those dimensions.
gate:
  minimum_object_improvement_pct: 3.0
  minimum_free_arm_improvement_pct: 2.0
  require_both: true
