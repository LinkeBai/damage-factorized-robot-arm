# Reproduction result notes

These small JSON files record local smoke tests, transfers and baseline
screens. They do not imply full reproduction of a paper's reported aggregate
performance.

The exact scope, upstream commits, missing datasets/checkpoints and dependency
boundaries are documented in
`reports/reproduction-first-audit-20260830.md`. Full upstream repositories,
downloaded datasets, checkpoints and replay buffers are intentionally excluded
from Git.
