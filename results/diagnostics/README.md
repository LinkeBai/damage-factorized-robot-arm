# Diagnostic result notes

These files preserve development screens and No-Go evidence. They are not
paper main-table results.

`sfet-original-arm-carrier-screen-20260830.json` retains the historical
machine label `oracle_ik` emitted by the frozen runner. That method did not use
future outcomes or hidden simulator truth; the corrected scientific name is
`fault-aware constrained IK baseline`. Reports and current plans use the
corrected name while the raw output remains unchanged for auditability.
