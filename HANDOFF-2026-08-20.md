# DFWM 项目交接文档 — 2026-08-20

> **状态更正（2026-08-21）**：本文主体记录 2026-08-20 当时的中间状态，已被
> `PROJECT-PLAN-V6.md` 顶部修订和 `reports/g2-final-synthesis-20260821.md` 取代。
> 后续五 seed 审计已否定 hypernetwork 的 K-shot 独立贡献；CR-GWM/RC-GWM
> 不具备可归因且跨 seed 稳定的预测优势；FT-GWM K2 与后续 FTC-WM Gate L 均未
> 解决 Push object/contact rollout。不得再执行本文“待完成实验”作为默认路线。
> 当前唯一主线为 ordinary ensemble + selective prediction，下一决策是是否以该
> 收缩主张进入 G3 真机验证。

## 项目概况

**目标**：让 DFWM（Damage-Factorized World Model）真正落地，投 ICRA 方法论文。

**核心思路**：把故障上下文因子化为 `c_damage = [e_topology（已知离散）+ z_residual（未知连续物理）]`，K 条校准轨迹推断 z，改善预测。

---

## 已完成的 G2 实验（正向结果，扎实）

| 实验 | 结果 | 文件 |
|---|---|---|
| 普通集成 vs 单模型 | +30.7%（5 seeds，bootstrap CI） | `results/final/g2_push_ensemble_5seed.json` |
| 拓扑条件化 vs 普通集成 | +2.47%，CI 跨零（**冗余**） | `results/final/g2_structured_vs_ordinary_5seed.json` |
| Held-out topology（D3 零样本） | +0.02%，CI 跨零（**无效**） | `results/final/g2_held_out_topology_5seed.json` |
| 成员数消融 M=1/3/5 | M=3 最优，M=5 边际 <1% | `results/final/g2_member_ablation_5seed.json` |
| 选择性预测（50% coverage） | **-51% RMSE**（极强信号） | `results/final/g2_selective_prediction_5seed.json` |
| 深度校准分析 | Spearman 0.93（全局）| `results/final/g2_depth_calibration_5seed.json` |
| 分歧指纹拓扑识别 | **K=1 → 100% 识别 D2/D3** | `results/final/route2_topo_id_5seed.json` |

---

## DFWM 落地尝试（均在 in-distribution 场景失败）

### 失败原因诊断

**根本问题**：普通集成训练时已经见过 D2/D3 所有物理，预测已经很好，任何形式的 z_residual 都是冗余的。Oracle（真实物理参数作为 z）比 ordinary 还差——WM 在训练中学会了忽略 z。

| 方法 | 结果 | 失败原因 |
|---|---|---|
| Latent optimization | K↑ RMSE↑ | 过拟合校准轨迹 |
| Amortized encoder（多版本） | z_norm ≈ 0.19 | Posterior collapse |
| 两阶段训练 | z_norm ≈ 0.17 | 同上 |
| 物理监督 + 对比学习 | z_norm ≈ 0.22 | 同上 |
| Topology Dropout | z_norm ≈ 0.40 | WM 仍忽略 z |
| 分歧指纹 + 结构化集成 | +0.01% | 结构化集成本身冗余 |

---

## 最新突破：超网络架构（2026-08-20）

### 方法

**DFWM-Hypernetwork**（`scripts/run_g2_dfwm_hypernetwork.py`）

- **OOD split**：训练只见 nominal+weak_motor，测试遇到 high_damping+delay_1（从未见过）
- **架构**：`z → HyperNet → 低秩权重偏移 ΔW = A @ B^T`，修正量 = `hidden @ (W_base + ΔW) + bias(z)`
- **两阶段训练**：Stage1 训练 WM_base（不用 z），Stage2 冻结 WM_base 只训练 HyperNet+encoder

### Seed 7 结果（smoke test）

| domain | base (ordinary) | dfwm K=0 | dfwm K=5 | oracle |
|---|---:|---:|---:|---:|
| D2 high_damping | 0.673 | 0.629 (+6.48%) | 0.630 (+6.38%) | 0.628 (+6.67%) |
| D2 delay_1 | 0.781 | 0.746 (+4.50%) | 0.747 (+4.37%) | 0.753 (+3.62%) |
| D3 high_damping | 0.761 | 0.735 (+3.42%) | 0.737 (+3.17%) | 0.760 (+0.12%) |
| D3 delay_1 | 0.860 | 0.835 (+2.89%) | 0.836 (+2.71%) | 0.818 (+4.82%) |
| **平均** | — | **+4.32%** | **+4.16%** | +3.82% |

**关键诊断**：改善存在且真实，但 K=0 和 K=5 差异很小（z_norm=0.11），说明主要贡献来自 HyperNet 的静态残差通道（W_base/b_base），而非 z 携带的校准信息。

### 当前状态

这是四天来第一个真实正向结果。**但 K 的贡献几乎为零**，需要进一步验证：
1. 是否需要去掉 W_base/b_base 静态通道，强迫所有修正经过 z
2. 还是接受这个架构贡献，重新定位论文

---

## 待完成的实验

### 优先级1：验证超网络结果稳定性（5 seeds）

```bash
for seed in 7 17 27 37 47; do
  python scripts/run_g2_dfwm_hypernetwork.py --seed $seed --epochs-s1 20 --epochs-s2 30
done
```

输出：`runs/g2_dfwm_hypernetwork/seed{N}_v1/`

### 优先级2：去掉静态残差通道，强迫 z 起作用

修改 `PhysicsHyperNet.__init__`：
- 删除 `self.W_base` 和 `self.b_base`（或者初始化为 frozen zeros 不可学习）
- 所有修正必须经过 z → A_gen/B_gen/bias_gen

修改后的 forward：
```python
def forward(self, z, hidden):
    A = self.A_gen(z_expanded).view(B, H, r)
    B_mat = self.B_gen(z_expanded).view(B, S, r)
    W = torch.bmm(A, B_mat.transpose(-1,-2))   # 不加 W_base
    bias = self.bias_gen(z_expanded)             # 不加 b_base
    return torch.bmm(hidden.unsqueeze(1), W).squeeze(1) + bias
```

如果这个版本 K=5 比 K=0 有 >2% 额外改善，则 DFWM 的 K-shot 贡献成立。

---

## 关键文件索引

| 文件 | 用途 |
|---|---|
| `scripts/run_g2_dfwm_hypernetwork.py` | 超网络 DFWM 训练+评估（**最新核心脚本**） |
| `config/experiment/g2_dfwm_ood_v1.yaml` | OOD split 实验配置 |
| `config/splits/g2_dfwm_ood_v1.yaml` | 训练：nominal+weak_motor / 测试：high_damping+delay_1 |
| `scripts/run_route2_overnight.py` | 分歧指纹拓扑识别流水线（5 seeds） |
| `scripts/collect_warp.py` | MuJoCo Warp GPU 批量数据采集 |
| `src/robotarm/models/amortized_encoder.py` | ResidualEncoder + InfoNCE + 物理监督 |

---

## 论文定位选项

### 选项 A：超网络架构贡献（如果 5 seeds 稳定）

主张：两阶段解耦 WM（拓扑 WM_base + 物理 HyperNet）在 OOD 物理下比单体 WM 改善 ~4%

弱点：K=0 和 K=5 改善相近，K-shot 校准价值尚未证明

### 选项 B：不确定性感知集成预测（稳健路线）

主张：
1. 普通集成隐式编码故障拓扑（分歧指纹 K=1 → 100% 识别）
2. 分歧幅度是可靠不确定性信号（Spearman 0.93）
3. 选择性预测 -51%（极强正向结果）
4. 拓扑条件化冗余（负结果，有诊断链）

优势：完整故事，正负结果都有，不需要新实验

---

## Codex 接力提示词

你是这个项目的接力工程师。目标是投 ICRA 2027，截止 25 天。

**当前最重要的任务（按优先级）：**

1. **跑 5 seeds 超网络实验**：
   ```bash
   for seed in 17 27 37 47; do
     python scripts/run_g2_dfwm_hypernetwork.py --seed $seed --epochs-s1 20 --epochs-s2 30
   done
   ```
   收集所有结果，计算 bootstrap 95% CI，如果平均改善 >3% 且 CI 不跨零，结果成立。

2. **验证 K 的贡献**：修改 `PhysicsHyperNet` 去掉 `W_base/b_base`（静态残差通道），只保留 z 生成的动态权重，重新跑 seed=7。如果 K=5 比 K=0 有 >2% 额外改善，DFWM 的 K-shot 主张成立。

3. **如果以上两步均成功**：写 5 seeds 汇总报告（bootstrap CI），开始论文写作，框架选项A。

4. **如果 K 贡献仍为零**：接受选项B，用已有结果写论文。

**项目架构简介**：
- 5-DOF push task，MuJoCo 仿真
- 故障：D2（关节2锁定）、D3（关节3锁定）
- 物理残差：nominal/weak_motor/high_damping/delay_1/noisy_deadband
- GPU 加速：mujoco-warp（RTX 4060 8GB）
- 训练框架：PyTorch，GRU world model

**运行环境**：
```bash
C:\Users\asus\PycharmProjects\mcm\.venv\Scripts\python.exe
cd C:\Users\asus\Desktop\damage-factorized-robot-arm
```

**关键诊断指标**：
- `z_norm > 0.5`：encoder 有效学习物理信息
- oracle 比 base 好：WM 能利用 z
- K=5 比 K=0 改善 >2%：K-shot 校准有价值
