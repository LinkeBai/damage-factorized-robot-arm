# Project Plan V6 — IPWM 真实机械臂闭环证据与论文收尾计划

**版本日期**：2026-09-06
**项目状态**：真实机械臂 Push-only 阶段已完成并封存；后续进入论文整合、仿真扩展与投稿审计。
**总裁决**：`PASS_READY_TO_DISMANTLE`
**适用原则**：本文件是当前唯一执行基线。较早计划、失败路线和现场过程保留在 Git 历史及证据目录中，不再作为当前待办。

---

## 1. 项目目标

研究低成本 5-DoF 机械臂在未知关节锁定故障下的视觉闭环操作。核心方法 IPWM 将故障描述、关节状态、视觉物体状态和候选动作序列用于反事实滚动预测，并在每个控制周期重新选择短动作，实现故障条件下的闭环 Push。

论文主张限定为：

1. 解析故障约束能够严格保持锁定关节状态；
2. IPWM 能在真实双摄像头观测与真实舵机反馈下持续进行候选评分、短动作执行和重新规划；
3. intact 与五种单关节锁定条件均已获得重复的真实闭环成功证据；
4. 双关节锁定实验显示方法既有可行域，也有稳定、可复现的失败边界；
5. 原始数据、逐周期决策、代码/模型身份、审计结果与备份均可追溯。

本文不声称动态闭环奖惩机制已经完成真机验证，不外推到跨机械臂、Grasp、任意视觉环境、全部组合故障或 SOTA 优势。历史固定轨迹 Push 不属于真实闭环。

---

## 2. 当前方法定义

### 2.1 IPWM 核心

IPWM 接收当前视觉状态、五关节反馈、故障 mask、锁定目标和候选关节参考序列，对候选动作后的状态进行多步预测。解析投影保证锁定坐标的位置和速度约束，学习模型预测自由关节、末端和物体响应。

每个真实控制周期执行：

1. 读取两路摄像头的最新帧；
2. 从 COM3 读取五关节反馈；
3. 构造不少于 10,000 个候选；
4. 在 NVIDIA RTX 4060 CUDA 上完成 IPWM 评分；
5. 保存候选库、代价、选择索引、推理耗时和模型身份；
6. 仅执行所选候选的短动作块；
7. 重新采集真实图像和关节反馈；
8. 达到成功门则停止，否则进入下一周期。

当前在线选择主要依赖预测终态代价。模型会滚动处理完整参考序列，但不能因此描述为已经训练完成的轨迹级动态奖惩方法。

### 2.2 真实观测和执行链

- 视觉：两个固定摄像头，保存双路原始视频与逐帧时间戳；
- 关节：COM3 五关节只读反馈；
- 推理：RTX 4060 CUDA；
- 控制：短动作块、重新观测、重新规划；
- 故障：单关节或双关节锁定，逐周期记录 fault mask；
- 记录：物块位置、目标误差、命令、反馈、候选、代价、选择、耗时、配置及代码/模型哈希。

---

## 3. 冻结的真实实验协议

### 3.1 核心单故障实验

- 条件：intact、D1、D2、D3、D4、D5；
- 任务距离：20 px，约 35.1 mm；
- 重复数：每条件 3 次；
- 正式总数：18 次；
- 起点门：图像坐标每轴 `±2 px`；
- 成功门：目标误差每轴 `±5 px`，连续 3 个不同真实帧；
- 每周期必须使用新图像与新关节反馈；
- 每周期必须评分不少于 10,000 个候选；
- 所有 GO、NO_GO、INVALID、RETAINED_ABORT 和复验均保留。

正式成员及裁决以 [final_push_formal_membership_v4.csv](data/real_robot/final_push_formal_membership_v4.csv) 为准。

### 3.2 双故障扩展

- 条件：J1+J2、J2+J3、J3+J4；
- 任务距离：15 px，约 26.3 mm；
- 仅将审计有效的真实闭环运行计入正式结果；
- 2026-09-04 首次 J1+J2 NO_GO 仅作为闭环管线验证；
- 早期 J3+J4 RETAINED_ABORT/INVALID 保留在全尝试清单，不进入正式统计。

### 3.3 历史实验边界

30/45/60 px 固定轨迹实验单列为 pilot 或 IPWM-guided open-loop 难度扩展。它们不与本批 20/15 px 闭环结果合并，也不用于计算正式闭环成功率。

---

## 4. 已完成的正式结果

| 证据组 | GO | NO_GO | 有效数 | 裁决 |
|---|---:|---:|---:|---|
| intact，20 px | 3 | 0 | 3 | 完成 |
| D1，20 px | 3 | 0 | 3 | 完成 |
| D2，20 px | 3 | 0 | 3 | 完成 |
| D3，20 px | 3 | 0 | 3 | 完成 |
| D4，20 px | 3 | 0 | 3 | 完成 |
| D5，20 px | 3 | 0 | 3 | 完成 |
| J1+J2，15 px | 1 | 0 | 1 | 可行 |
| J2+J3，15 px | 1 | 0 | 1 | 可行 |
| J3+J4，15 px | 1 | 2 | 3 | 有条件可行，保留失败边界 |

核心结果为 **18/18 GO**。正式成员总数为 23，`PENDING=0`。共保留 29 个真实试验目录，且 29 个目录均进入全尝试索引。

D4-03 与 D5-02 的成功停止发生在达到 goal gate 后。执行器可以取消最后一个尚未执行或只部分执行的短动作；审计已按时间顺序、连续真实帧和 goal guard 核验，两次均为合法 GO。后来新增的日志字段不用于倒推否定早期有效数据。

---

## 5. J3+J4 失败边界与方法发现

J3+J4 的三次正式运行包含两次 NO_GO 和一次 GO。该组不能只报告最终成功，必须完整呈现三次有效运行。

诊断显示：

1. 严格高度与姿态候选门抑制了 J2 前伸；
2. 候选分配过度依赖 J5；
3. 对 J5 施加强惩罚后，真实物块仅推进 0.26 px；
4. 恢复有效 J5 推进并保持持续闭环重规划后，第 31 个周期达到 GO。

这说明问题不仅来自世界模型误差，也来自候选生成、关节分配和代价设计之间的耦合。论文应将其写成可观测的控制失败边界，并作为轨迹级动态奖惩扩展的动机，不能把它包装成已经验证的新方法收益。

---

## 6. 真实性审计与封存

### 6.1 权威文件

- [完成审计](reports/ipwm_final_submission_archive_ready_20260905/completion_audit.json)
- [正文结果与失败边界](reports/ipwm_final_submission_archive_ready_20260905/submission_results.md)
- [正式成员表](data/real_robot/final_push_formal_membership_v4.csv)
- [总封存审计](results/real_robot/final_push_total_closure_audit_20260905.json)
- [封存回执](results/real_robot/final_push_archive_receipt_20260905.json)
- [代表帧目录](reports/ipwm_final_submission_archive_ready_20260905/representative_frames)

### 6.2 审计结论

- 总状态：`PASS_READY_TO_DISMANTLE`；
- 核心：18 行、18 GO；
- 正式成员：23；
- PENDING：0；
- 保留试验目录：29；
- 全尝试索引：29；
- 物理关机与断力：已验证；
- 审计错误：0。

代表帧使用确定性的最后解码俯视帧，不进行人工挑选。

### 6.3 双备份

- 备份 A：`C:\Users\asus\Documents\IPWM_FINAL_PUSH_BACKUP_A_20260905`
- 备份 B：`C:\Users\asus\Desktop\IPWM_FINAL_PUSH_BACKUP_B_20260905`
- 文件数：9,994；
- 总字节数：14,260,676,160；
- manifest SHA-256：`4c49b2a3a1a360adb46585fb44c6bd7cdedd3f64f688b3e8ae41df67dc8c2e03`；
- 两份备份均已与冻结源核验；
- 复制与核验过程中源数据未变化。

---

## 7. 论文贡献组织

论文主线按以下因果顺序展开：

1. 关节锁定改变可行动作空间和接触动力学；
2. 解析投影严格满足锁定关节约束；
3. IPWM 对故障条件下的候选动作序列进行反事实滚动预测；
4. 真实系统每周期重新感知、评分、短执行和重规划；
5. 逐周期候选、原视频、反馈、哈希和全尝试清单形成可审计证据链；
6. J3+J4 揭示候选分配与代价设计的失败边界；
7. 轨迹级动态奖惩在仿真中验证，作为辅助扩展。

正文真机表直接报告 18 次核心运行和 5 次双故障有效运行。J3+J4 的 2 次 NO_GO 必须与 1 次 GO 同表呈现。

---

## 8. 动态闭环奖惩扩展

### 8.1 研究问题

终态代价是否忽略“如何到达终点”的过程信息，并因此导致不合理的关节分配、接触中断或横向漂移？

### 8.2 候选机制

轨迹级累计代价由以下部分组成：

- 终点目标误差；
- 沿推轴的逐步进展；
- 横向漂移；
- 接触连续性；
- 关节余量与关节分配；
- 锁定关节漂移；
- 预测与真实观测残差。

权重或奖励调度只能使用当前及历史可获得信息，不能使用未来真实轨迹或评测终点标签。

### 8.3 冻结消融

统一数据、候选预算、种子、故障 split 和评测协议，比较：

1. terminal-only；
2. trajectory-level fixed reward；
3. trajectory-level dynamic reward；
4. dynamic reward 去掉接触项；
5. dynamic reward 去掉关节余量项；
6. dynamic reward 去掉预测—真实残差项。

主要指标包括终点误差、成功率、候选 regret、横向漂移、接触连续率、闭环周期数、推理时间、关节余量和锁定漂移。

该扩展先在仿真完成。若只作为辅助和拓展，不需要追加真机实验，论文标注为 simulation-only auxiliary extension。只有将其升级为主要真机贡献时，才建立新的冻结真机协议。

---

## 9. 统计与报告规则

1. 不删除失败、中止或无效目录；
2. 不用最终结果反向调整正式成员；
3. INVALID 不计入成功率分母，但必须在全尝试清单披露；
4. pipeline validation 不混入正式闭环统计；
5. 不混合不同任务距离与不同控制范式；
6. 报告每个条件的原始计数；
7. 小样本双故障结果使用描述性统计；
8. 仿真主比较使用配对设计、多个种子和置信区间；
9. 所有论文数字必须追溯到机器可读结果；
10. 图、表、视频索引和正文使用同一正式成员表。

---

## 10. 论文交付物

### 方法部分

- IPWM 架构图；
- 解析约束投影公式；
- 真实闭环时序图；
- 候选生成、评分和短动作执行伪代码；
- 终态评分与轨迹级奖惩扩展的关系。

### 实验部分

- 仿真环境、故障 split 和基线；
- intact 与 D1–D5 真机结果表；
- 双故障结果表；
- J3+J4 完整失败边界；
- 代表帧与视频索引；
- 推理耗时和候选预算；
- 局限性与非主张清单。

### 投稿前审计

- 数字与成员表一致；
- 引用对应原始来源；
- 图表与正文口径一致；
- 匿名信息清理；
- PDF 页数、字体、裁切和可读性检查；
- 代码、模型、配置和结果哈希写入复现说明；
- 不将仿真扩展误写为真机验证。

---

## 11. 当前执行顺序

### P0：论文真实结果整合

- 将 18/18 核心 GO 和双故障结果写入正文；
- 生成正式结果表、失败边界图、代表帧图和视频索引；
- 在方法部分明确真实闭环的逐周期数据流；
- 将历史 open-loop 结果移入补充材料或独立小节。

**完成条件**：正文所有真机数字与正式成员表和完成审计一致。

### P1：动态奖惩仿真消融

- 实现轨迹级代价；
- 冻结比较协议；
- 完成多种子消融；
- 报告性能、稳定性和计算开销；
- 根据证据决定保留为辅助扩展或升级为方法组成。

**完成条件**：所有预定方法和消融均有机器可读结果，且不存在 PENDING。

### P2：整稿一致性与投稿审计

- 更新摘要、引言、方法、实验、局限性和结论；
- 完成事实、统计、引用和匿名性审计；
- 重新渲染并检查最终 PDF；
- 生成最终论文包与复现索引。

**完成条件**：论文中的每项主张均有对应证据，最终审计无阻塞错误。

---

## 12. 项目完成定义

真机阶段已经完成，不需要为旧实验追加条件或用新版规则追溯作废。

整个投稿项目在以下条件同时满足时完成：

- 真机结果、失败边界和证据链写入论文；
- 动态奖惩仿真扩展完成或明确裁决为不纳入；
- 主表、补充材料、代表帧和视频索引生成；
- 全文事实、统计、引用、匿名性和版式审计通过；
- 最终代码、模型、配置、结果与论文包完成哈希封存和双备份。

当前状态统一表述为：**真机阶段完成，论文与仿真收尾进行中。**
