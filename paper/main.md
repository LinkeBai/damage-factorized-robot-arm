# ICRA paper workspace

The active manuscript is paper/main.tex; paper/main.pdf is its compiled
preview. The former DFWM latent-optimization draft was retired because that
mechanism failed the project gates and no longer describes the frozen method.

Completed: abstract, introduction, expanded and audited related work, problem
formulation, method equations and deployment sequence, a vector architecture
figure, exact implementation details, matched three-seed simulation tables,
the long-horizon mechanism figure, confirmation discipline, ablations,
safety/control checks, threats to validity, reproducibility, limitations,
conclusion, and references. The current compiled manuscript is five pages.
Adding only the real-arm evidence is expected to bring the submission to
six--seven pages while remaining below the eight-page limit.

The only open placeholders are real-arm evidence: setup photograph and
calibration panel, intact/D2/D3 pushing table, measured safety events, and the
real-arm conclusion sentence. These fields must be filled from measurements;
the manuscript contains no fabricated real-robot values.
